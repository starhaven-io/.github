<!--
Replace this comment with a concise summary of the change: what and why. Keep
each prose paragraph on one source line. Do not add a standalone test-plan
section or checklists.

If AI/LLM was used to generate or assist with this pull request, the initial PR
description must end with exactly one visible, unformatted line outside this
comment: `AI disclosure: <model> with <how the output was verified>.` Name the
model as its vendor names it, for example `Claude Opus 5`. Do not also name a
tool or harness unless the harness is the only identifier, and do not describe
what the AI did. This is PR body text, not a commit trailer. Do not format the
disclosure as a heading, bullet, bold label, or horizontal rule, and do not add
a promotional "generated with" footer. Omit the line when no AI/LLM was used.
Anything left inside these comment markers does not render, so a disclosure
written here would not be published.
-->
