name: Fleet Guard

on:
  pull_request:

permissions: {}

concurrency:
  group: "fleet-guard-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  guard:
    name: Guard
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-fleet-guard.yml@5684a11c4f6cbcea9c0fc24fdd5bb6bd602e32c3 # v2026.07.05.8
    with:
      hub-ref: "393adbb1228f90d27d13093b4eb8b2a6f602d7fc"
      repo-name: ${{ github.event.repository.name }}
