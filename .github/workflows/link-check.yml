name: Link Check

on:
  pull_request:
    paths:
      - "README.md"
      - "profile/README.md"
      - "CONTRIBUTING.md"
      - "SECURITY.md"
      - "lychee.toml"
  schedule:
    - cron: "0 14 * * 1" # every Monday at 14:00 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: "link-check-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  lychee:
    name: Check links
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-link-check.yml@7917b6404c4b6e9f2bffc8aa4f6b508d06edb897 # v2026.07.29.1
    with:
      args: "--config lychee.toml README.md profile/README.md CONTRIBUTING.md SECURITY.md"
      build-site: false
      site-directory: ""
      authenticated-github: true
